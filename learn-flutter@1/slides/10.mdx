# Flutter (1)

```dart
Container(
  padding: EdgeInsets.all(16),
  child: Column(
    children: [
      Text("Trainings"),
      Image.asset("images/trainings.png"),
      SizedBox(height: 16),
      Row(
        children: [
          Text("latest trainings"),
          Spacer(),
          Icon(Icons.more),
        ]
      ),
    ]
  ),
);

```
