# Dart (1)

0. Loops and conditions
```dart
for (String address in addresses) {
  // a set of instructions
}

if (isStructured) {
  // a set of instructions
} else {
  // a set of instructions
}
```

1. Functions
```dart
double calc() {
  // a set of instructions
}
String upper(name) => name.toString().toUpperCase();
```

2. Classes and Objects
```dart
class Client extends User {
  String name;
  DateTime createAt;
  bool verify() {
    // a set of instructions
  }
}
```

3. Types
- `bool` authenticated = `false`;
- `int` count = 34;
- `double` width = 21.75;
- `String` country = "Algeria";
- `List` sizes = [12, 16, 21, 39];
- `Map` phone = `{`"constructor": "Samsung", "model": "Galaxy A6", "year": 2018`}`;
