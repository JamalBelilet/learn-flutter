# Code (0)

```dart
import 'package:flutter/material.dart';

void main() {
  runApp(DEVTO());
}

class DEVTO extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: HomePage(),
    );
  }
}
```
